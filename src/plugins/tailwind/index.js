import { default as writingMode } from './writingMode';
import { default as animationDuration } from './animationDuration';

export const CustomPlugin = {
  writingMode,
  animationDuration,
};

export default CustomPlugin;
